
import React from 'react';
import { Link } from 'react-router-dom';
import { cn } from '@/lib/utils';

// Empty Navbar component that doesn't render anything
const Navbar: React.FC = () => {
  return null;
};

export default Navbar;
